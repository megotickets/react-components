module.exports = {
  plugins: {
    // Non includiamo 'tailwindcss' qui.
    // Lasciamo l'oggetto plugins vuoto per ora.
    // Se in futuro scoprissimo che @megotickets/core necessita
    // specificamente di altri plugin PostCSS (es. autoprefixer),
    // potranno essere aggiunti qui.
  }
}; 