{
  "name": "@mego/components",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --watch components --ext ts,tsx --exec \"rm -rf dist && rm -rf node_modules && yarn && yarn build && cd test && rm -rf .next && rm -rf node_modules && yarn && yarn dev\" --ignore dist/ --ignore node_modules/",
    "clean": "rm -rf dist && rm -rf node_modules"
  },
  "dependencies": {
    "@rainbow-me/rainbowkit": "^2.2.3",
    "@tanstack/react-query": "^5.66.0",
    "axios": "^1.7.7",
    "ethers": "^6.13.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "nodemon": "^3.0.0"
  },
  "peerDependencies": {
    "react": ">=19.0.0",
    "react-dom": ">=19.0.0"
  },
  "files": [
    "dist",
    "package.json"
  ]
} 